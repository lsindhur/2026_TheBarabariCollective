const arr = [2, 4, 77, 66, 13, 17, 19, 33, 45, 67];

//use array helper methods
//do not use a for loop
//loop through the array 
// and print even numbers in new array
//map, foreach, filter, find, push, pop, shif...

//[2,4, 66]

let result = arr.filter(ele => ele%2 ===0)
console.log(result)

/* let result = [];
arr.forEach(ele => {
    if(ele%2 === 0) {
        result.push(ele)
    }
})

console.log(result) */




